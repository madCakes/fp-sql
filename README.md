# SQL

Some example code for helping to teach people how to work with SQL.

## Set-up and run
- Clone this repo
- `cd fp-sql`
- `docker compose up -d`
- `docker exec -it postgres bash`
- `psql -U postgres`